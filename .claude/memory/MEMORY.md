# Memory Index

- [每次改动跑 lint 三件套](post-change-lint-chain.md) — 用户强原则(2026-09-04):每次代码改动后必跑 `bunx eslint .` + `bunx nuxi typecheck` + `bun run tailwindcss:lint`,三件套并列、不是提交前才跑;tailwindcss:lint 与 eslint/typecheck 一组
- [dev server 3000 勿杀](dev-server-3000-dont-kill.md) — 用户可能自起 dev server 在 3000,**绝不 kill/占用已在跑的端口**;要自起用别的端口(如 3001,先 netstat 确认);测试直接访问用户已跑的那个。呼应 audit-skill 的 dev server 记录
- [死代码扫描:自动导入前缀盲区](dead-code-scan-autolimport-prefix.md) — 扫未用 Vue 组件时,组件可能被 Nuxt 自动导入的目录前缀/`Lazy`前缀引用(`<AdminTravelCoordinatePicker>`/`<SiteActivityHeatmap>`/`<LazyContextMenu>`),只看无前缀名会误判活组件为死(ContextMenu 曾被误删靠 git 恢复);eslint 抓不到(自动导入无 import);全局 grep 组件名或 knip
- [markdown 行内链接域名图标](markdown-link-chip-display.md) — 已知域名(GitHub/Gitee/百度/谷歌/腾讯QQ/微信/Mozilla-MDN/npm)行内链接在 link_open 文本前插入空 `<span class="markdown-link-icon markdown-link-icon--<slug>">`(图标 CSS mask+currentColor);链接不加 class 保持普通超链接外观;样式在 [slug].vue 与 agreement.vue 各一份 scoped;`:::repo` 仓库卡片走 Vue 组件不受影响;**扩展 LINK_CHIP_DOMAINS 须具体子域排父域前**(weixin.qq.com/wx.qq.com 先于 qq.com,否则 mp.weixin.qq.com 因 endsWith(".qq.com") 错配成 tencent)
- [Nuxt SSR/API 双 bundle 单例坑](nuxt-ssr-api-bundle-singleton-memory-session.md) — server util 模块级单例被 app/nitro 两个 bundle 各内联一份,memory session 互不可见→/admin 整页进入 302 跳登录又自动回;file/database 走共享存储不受影响;修复=单例挂 globalThis;curl 对比 /admin(302) vs verify(valid) 即暴露
- [Redis 构建期烘焙+Docker 拆两套显式版本](redis-build-baked-docker-ondemand.md) — Redis 只读构建期 env(shared/redis-config)→烘焙 runtimeConfig.redis(nitro ISR 共用),运行时零 Redis 环境变量;Docker 拆 docker/ 两套显式版本(带/不带 Redis),无 COMPOSE_PROFILES,运行统一 `--env-file .env -f docker/...`;.dockerignore 锚定 /Dockerfile 防误排子目录;env_file 惰性注入 REDIS_* 无害
- [Tiptap Link inclusive 是 schema 静态字段](tiptap-link-inclusive-schema-field.md) — inclusive() 写死=autolink(true),configure 改不动(core 只读 extension field),必须 Link.extend({inclusive:()=>false});inclusive:true 致链接末尾输入延续 mark="建链后无法输普通文本";已应用链接无取消入口→弹窗加「移除链接」(unsetLink)+url 留空确定也移除
- [composable 嵌套 ref 模板不解包](composable-nested-ref-template-no-unwrap.md) — Vue 规则:composable 返回对象里的 ref/computed 模板不解包,须解构成顶层 ref(示例 useTextareaHistory 已删,但规则适用;EmojiRichInput 消费返回对象需 .value);解构 ref 不丢响应式
- [cn 是纯 clsx 无 tailwind-merge](cn-is-pure-clsx-no-tailwind-merge.md) — 本仓库 cn=clsx 只拼不去重,`cn("flex","block")` 两者并存、靠 Tailwind CSS 源序决胜(非类名顺序);要压调用方透传的 Tailwind 类用 scoped CSS(`.foo[data-v-x]` 0,2,0 > `.flex` 0,1,0)或直接改传入串。附:Vue 缺省 Boolean prop=false 非 undefined,要默认 true 须 withDefaults
- [页面过渡可见性靠 Suspense](page-transition-pjax-origin.md) — app.vue mainOpacity 时长靠 page:start/page:finish 钩子补满 fadeDuration,但旧页渐出可见性依赖新页顶层 await 挂起 Suspense;同步 setup 渐出失效,要"不发请求又渐出"须人为 await fadeDuration(仅 SPA 导航);加位移 mainTranslateY 时首页/地图等全屏固定页与全局 transform 冲突→涉及这些页(NO_TRANSLATE_PATHS=['/','/map'] 预算 navSkipsTranslate)全程 opacity-only,稳态 transform 必须为 none 否则 translateY(0) 包含块破坏后代 fixed
- [SFC onMounted 裸 await 编译坑](sfc-onmounted-await-gotcha.md) — [slug].vue 等 SFC 的 onMounted(async) 体内裸 await 会让 nuxt build 报 Unexpected reserved word 'await';用 async IIFE/async forEach 回调包裹
- [DB 迁移历史与本地脱节](db-migration-disconnected.md) — DB 用另一套 init,本地 migrations 全"未应用",实际靠 db push/原始 SQL 同步;禁用 migrate dev/reset(会 reset 丢数据),schema 变更走 db execute/自写 tsx 幂等脚本
- [API 类型走 Nitro InternalApi](api-types-nitro-internal.md) — 端到端响应类型用 Nuxt 内置 InternalApi,旧 api:types 生成器已删;nitro 只推断响应不生成 query/body,入参要类型化给 handler 加 zod 局部 z.infer,别再造全局生成脚本
- [ESLint 严格模式已落地](eslint-adopted-strict.md) — @nuxt/eslint flat config + stylistic(无 Prettier);根无 lint/lint:fix 脚本,校验用 bunx eslint .(小程序 mini:lint),eslint.config.mjs ignores=[scripts,node_modules,mini];已 exit 0 零残留,刻意保持 error 驱动清理,别降级为 warn
- [根无 lint/type-check 脚本](lint-typecheck-no-root-script.md) — 根 package.json 只有 build/dev,lint/type-check 脚本只在 mini/;bash CWD 易残留在 mini 导致验错项目。验 Nuxt 改动先 `pwd`,在根 `bunx eslint .` / `bunx nuxi typecheck`(根 vue-tsc 不走 .nuxt/tsconfig.json 会漏报类型,实测误判 0 错误)
- [类型必须放独立文件](types-in-dedicated-files.md) — 不在 .ts/.vue 内内联定义 interface/type;服务端 server/types/apis/*(#server 导入),前端 app/types/apis/*(~ 导入),新增结构先建类型文件再 import
- [definePageMeta 页面勿当组件引](definepage-meta-page-as-component.md) — 404.vue 曾 import [...slug].vue 当组件,触发 definePageMeta 运行时警告(宏在组件上下文不提取);复用 UI/逻辑抽 components/ 或 composables/,不页面互引
- [async composable 内部 await 丢实例](async-composable-loses-nuxt-instance.md) — async composable 体内 await 后再调 use* 会报 "composable called outside...setup";返回 Promise 给页面顶层 await,或把同步 composable 调用挪到 await 之前
- [滚动监听首帧须延后到 onMounted](scroll-listener-hydration-onmounted.md) — 读 window.scrollY 写 isScrolled/进度等状态的 composable,首次同步执行须在 onMounted(水合后),setup 同步赋值在 scroll restoration 下触发 hydration class mismatch;addEventListener/订阅可 setup 同步做但首次回调放 onMounted
- [小程序实况照片点击播放](mini-live-photo-click-play.md) — mini 端 Motion Photo:拉整包扫 ftyp 切 MP4,<video> 覆盖静态图点击播放;视频段 H5 用 blob/小程序 writeFile 临时文件,用 IS_H5+运行时 if 分流(勿双 return);#live 锚点标 ImageBlock.isLive
- [Prisma 关系键重命名坑](prisma-relation-key-rename-trap.md) — 批量改标识符时 Prisma where/orderBy 的关系键(join 模型上是单数 content,非 contents)会被误改,vue-tsc/build 全绿也抓不到,只运行时 500;改完必须启 dev 逐个 curl API(尤其 category/tag/mini/footprint/travels)
- [公开接口显式白名单字段](public-api-explicit-field-whitelist.md) — 用户强原则:前台不漏任何非必要字段(隐私+内部审核字段都算泄露);公开接口 findMany 必带 select 或响应节点逐字段构造,禁 ...row;已修 comments.get/links.get,范式见 mini/*;必 curl grep 验
- [Admin 写接口必须 CSRF](admin-write-api-csrf-required.md) — /api/admin/* POST/PUT/DELETE 必带 validateCsrfToken;POST/PUT 从 body csrfToken、DELETE 从 header x-csrf-token;referer-check 非替代(开发环境跳过);tags CRUD 曾整体漏(2026-07-16 修),新增写接口别漏
- [Admin 写接口 catch 别全吞 500](admin-write-catch-swallow-pattern.md) — catch 无条件抛 500 会盖掉 validateXxxData 的 400 和 Prisma P2025 的 404;范式:带 statusCode 的错误原样抛 + `error.code==="P2025"`→404,参考 tags/[id].put.ts:57 + upload.post.ts:282;links/subscribes 已修,后续 changelogs/travels/attachments 大概率同类
- [前台动态路由 not-found 须 setResponseStatus(404)](frontend-soft404-setstatus.md) — category/tag/content 页 useFetch 收 404 只设 error ref、不抛错,内联 <NotFound> 渲染成功 → SSR 返 200 soft-404;isNotFound 后加 `if(import.meta.server){if(isNotFound.value) setResponseStatus(useRequestEvent(),404)}`;seoMeta 早返 {} 还会让 not-found 文档无 <title>,应返 {title:pageTitle.value}
- [auth 会话层安全不变式](auth-session-security-invariants.md) — sessionId/authCode 必用 crypto.randomBytes(非 Math.random,否则可预测);login.post 响应字段白名单(别漏 authCode);server/utils/login-rate-limit.ts 按 IP 内存限流(单实例局限);登录页 redirectTo 仅站内路径防开放重定向;hasUser 并入 /api/auth/verify(原 status 探针口已删)
- [auth 回跳用 fullPath 不是 path](auth-redirect-fullpath-not-path.md) — route middleware 登录回跳用 to.path 丢 query(cid 没了)→ 登录后回无 cid 新建页/编辑器空载;症状(空编辑器)不指向重定向中间件,排查先看 URL bar 的 cid;改 to.fullPath 且别字符串拼 encodeURIComponent(navigateTo 经 ufo round-trip 会吃掉编码),用对象式 navigateTo({path,query:{to:fullPath}})
- [列表页 apiSlug 只初始化不 watch](usefetch-route-param-spa-refetch-trap.md) — category/tag [slug] 重挂载→apiSlug 只 `ref(slug.value)` 初始化、删 watch(slug) 与 watch(route.params.slug);旧 watch 致旧页存活期 useFetch 重请求清空 data→articles-grid(v-if contents>0)移除→flex justify-center 把孤立标题(menu/# 图标)居中屏幕中央,且致文章页 404 误请求;切分类动画靠新实例 onMounted→triggerEntryFadeIn
- [媒体直链 http 被 CSP 拦(Firefox)](media-csp-https-only-firefox.md) — media-src/img-src 故意无 http:,网易云等直链返回 http 在 Firefox 被 CSP+混合内容双拦→连续失败 disablePlayer 停用页脚音乐;后端 /api/meting url/pic 重定向须 toHttps 升级;Chrome 静默升级故无症状
- [LivePhoto Firefox HEVC 降级](livephoto-firefox-hevc-degrade.md) — 线上实况照片多为 single ftyp + HEVC,Firefox 不解(仅原始一加照片才双 MP4),video @error 降级静态图已生效;LivePhoto.vue + useFancyboxLivePhoto.ts 监听 video @error 清 videoBlobUrl/移除 DOM 退化为静态图(降级非根治);要 Firefox 也能播需服务端转码接 compress 脚本
- [页脚音乐架构 useAudioPlayer](footer-music-architecture.md) — FooterMusic 不用 APlayer,走 useAudioPlayer(原生 new Audio,detached 不入 DOM → querySelector('audio') 为 0 正常);md:hidden/max-md:hidden 双实例响应式;meting 后端 toHttps;.aplayer-container 是别处干扰项
- [动态路由切参数过渡](nuxt-page-key-route-path.md) — ❌纠正早先"复用"判断:[slug].vue 顶层 await useFetch → 异步 setup → NuxtPage Suspense 每次导航重挂载(onMounted 每跳触发、旧 .articles-grid 断开);渐出靠 Suspense 挂旧页等新页 await;勿加 :key/definePageMeta key;旧 pageDataReady/viewData/onBeforeRouteUpdate(Option A)对重挂载冗余死代码(watch(slug)/watch(route.params.slug) 有害已删,致 menu 图标居中→见 usefetch-route-param-spa-refetch-trap);文章列表下跳=重放 translate-y-8 上滑;当前(2026-08-23)是实例级 hasPlayedEntryFade 仍重播,『仅首播+后续原位』需模块级会话开关但 useCategoryEntrySlide 方案未落地
- [QQ音乐听歌数据拿不到](qqmusic-personal-data-unreachable.md) — 个人动态页音乐数据源调研:听歌排行/播放历史被锁 App 端,官方开放平台(developer.y.qq.com)无个人接口;主流逆向 jsososo/QQMusicApi 读 routes/user.js 全源码只到歌单/收藏/关注(/user/songlist 等),无"我的听歌排行/最近播放"接口(issue#167 Closed);可行方案=歌单逆向+个人 cookie(非听歌数据)/Last.fm scrobble 中转(官方API)/手动录 admin
- [小程序评论表情混排](mini-inline-emoji-mix-render.md) — mini 无 v-html 且 <text> 不能嵌 <image>,行内表情靠 parseCommentContent 切 text/emoji token 分段渲染(image inline-block+vertical-middle);/emojis 路径须 siteConfig.siteUrl 补全;mini 无 shared/app 别名,emojis.json 与 EMOJI_CATEGORIES 手动同步复制(capoo 是 gif 扩展名不统一,必查表不能从 key 推 path)
- [小程序页面标题左边距](mini-page-title-bar-padding.md) — 列表/聚合页 `.title-bar` 左右 padding 统一 24rpx(40 24 8);卡片式头部(messages 的 intro / content detail 的 header)自成 40rpx 体系不纳入;新增列表页别再写 40rpx
- [admin 确认弹窗走 useConfirm](admin-confirm-useconfirm-not-native.md) — 后台确认弹窗统一 `useConfirm()` + 全局 `<ConfirmDialog/>`(挂在 AdminLayout),禁原生 confirm()/alert();`await confirm({title,description,variant:'destructive',confirmText,icon})` 返回 Promise<boolean>,近 drop-in 替换;2026-08-03 迁完 13 页 17 处
- [Tiptap markdown 往返四坑](tiptap-markdown-roundtrip-gotchas.md) — MarkdownEditor.vue 改 Tiptap:① `@tiptap/extension-image` v3 默认 block,独立图片序列化时与相邻段落挤一行,修 `Image.configure({inline:true})`;② 测往返保真别用 `editor.commands.setContent(fullMd)`(tiptap-markdown 覆盖它,整串当 md 解析、绕过 splitMarkdown 损坏 `:::` 容器),要走真实 loadMarkdown 路径;③ StarterKit CodeBlock 保留 language 但 addCommands 无改语言入口,`CodeBlockLowlightWithLang.extend({addNodeView})` 加 Vue NodeView(pre 右上角纯文本框)改 language,extend 不动 schema 故往返不受影响(早期 input+datalist 已弃)。容器须 0 差异,表格对齐空格/linkify/代码块尾空行 trim 等属可接受渲染等价差异;④ editorProps.handlePaste 必须 sync,async 返回 Promise 被 ProseMirror someProp 当 truthy→doPaste 跳过默认粘贴事务致普通文本粘贴全失效,改同步签名+fire-and-forget 异步上传(配 suppressEmit 挂起回写防占位 URL 落库、getAsFile null 清占位)
- [MarkdownEditor 固定高度+内部滚动](markdown-editor-fixed-height-internal-scroll.md) — 根固定高度现为 flex-col + editorHeight 拖拽持久化(localStorage,默认720/min320/max3000),非 calc(100vh-200px))+编辑区 flex-1 overflow-auto 内部滚动(修掉 scrollTop 恒0/滚动跑 window 层);工具栏在 flex 头部天然固定无需 sticky/fixed(Card.overflow-hidden 会截断 sticky);表格上下文 v-if 整体切换工具栏内容(废弃旧 fixed 浮层+~60行定位代码);eslint 未启 html-indent,v-else 内按钮不+2缩进不报错
- [滚动渐入+SVG图表两坑](page-reveal-and-svg-chart-gotchas.md) — ①抄 about.vue 的 `.ready{opacity:0}`+`.fadeIn{backwards}` 滚动渐入必须配 `animationend` 移除 .ready,否则播完回 opacity:0 整页透明;②图表别用 SVG `<rect>`/`<text>` 动态绑 :x/:y/:height,Vue hydration 走只读 setter 报错中断 onMounted→整页空白(症状"不显示"但 SSR HTML 在),改 div+CSS
- [代码字体 family 名须对齐 @font-face](font-mono-jetbrains-name-trap.md) — @font-face 注册无空格 `JetBrainsMono`(public/fonts/font.css 分 ASCII/Symbols/Rest 三片,Rest 含 Nerd Font 图标 PUA 字形);引用写成带空格 "JetBrains Mono" 或无引号 `JetBrains Mono, monospace`(解析成两 token)→匹配不上→代码块回退非等宽+图标豆腐块。Tailwind v4 preflight 经 --default-mono-font-family 把 pre/code 绑到 --font-mono,改 main.css --font-mono 一处全局生效(前台代码块也走它非继承 body);回退栈要真等宽 ui-monospace/SFMono/Menlo/Consolas/monospace
- [TravelMap 初始化 zooms 上限固定 20](travelmap-maxzoom-sync-createcluster.md) — 高德底图 POI 文字图层(公园/建筑/街道名)可见级别在**初始化时**按 zooms 上限锁定,运行时 setZooms 放开无效;故 `new AMap.Map` 的 zooms 上限必须固定 20,**不能**写 `props.maxZoom??20`(从访客分布进入会初始化成 [4,9]→切我的足迹放大后几十个 POI 全消失,只剩大头针)。footprint/blogs 的 9 级封顶改由 createCluster 内 `setZooms([lo,effectiveMax])`(与 maxZoom:effectiveMax 同源)运行时实现,setZooms 收窄不影响 POI。setZooms 须在 createCluster 而非独立 maxZoom watch(切视图 places 懒加载先变空→cluster 销毁无点,watch 提前放开成「无点却允许深缩放」窗口);maxZoom watch 只保留越界钳制。调试:`document.getElementById('travel-map').amap` 即 map 实例
- [AMap marker 须等 complete 再建 + tab 切换点消失](amap-markers-before-complete-deferred.md) — ① complete 前建的 marker/聚合推迟到地图首个渲染周期才画→「底图先出、点后冒出」;初次建聚合放 complete 回调(fit→建→loading=false 同帧),兜底 timer 与 complete 走同一幂等 init,已修 TravelMap.vue + TravelCoordinatePicker.vue。② tab 切换点消失:切未加载过的视图 places 先空→旧聚合销毁;map.vue 用 lastNonEmptyPlaces shallowRef 在 pending 期顶住旧点(引用不变→TravelMap places watch 不触发不销毁)+半透明加载遮罩(pending&&shouldShowMap)+失败遮罩(error&&shouldShowMap,retryCurrentView 重试)
- [库名 imqi1-cms](imqi1-cms-db.md) — 库名 imqi1-cms(与目录/包名一致,旧名 imqi1-nodejs/nodejs-imqi1 已统一改掉);本地已装 MySQL 8.4.9 便携版(C:\mysql-8.4\mysql-8.4.9-winx64),DB_* 由 .env 提供,开机自启在 Startup;初始化 bun run db:init 幂等(建 15 表+种子,admin/123456);呼应禁 write/migrate/reset
- [/audit Skill 体检流程](audit-skill.md) — 约定+真bug+用户拍板+实测一条龙(与 /review 只扫约定、/check 只 lint/typecheck 分工);固化 dev server 绑 [::1]:3001、登录 admin/123456、chrome-devtools 锁报错回退 playwright;测试产物 .playwright-mcp 已 gitignore
- [app/lib/aplayer 重构版三坑](aplayer-lib-gotchas.md) — APlayer TS 重构:模板 .art→.ts 丢 HTML 转义→DOM XSS(改模板加 escapeHtml,icons 勿转义);异步歌词 XHR 竞态致切走再回永久 Loading(占位不入缓存+destroy abort);拖拽打啵(pointercancel/blur 兜底)卡 disableTimeupdate+监听泄露;eventClientX 勿用 ||、bar.set 防 NaN%、Storage volume 用 ??
- [admin 页面审计九坑](admin-pages-audit-gotchas.md) — app/pages/admin 全量审计(workflow+核验):csrfToken 空仍发写请求(写函入口 !csrfToken 守卫+服务端是硬防线);双击重复提交(submitting/deleting ref);列表请求竞态(fetchSeq 序号);URL page 负数 Math.max(1,..);content-categories 缺 metas type:'category' 把标签混进分类(对齐 content-tags);status \|\| 吞 0(草稿显已发布);破坏性确认全 useConfirm;页面/文章判定读 content.type 别靠 pageSize=999;cache map→/map 勿误伤 sitemap
- [validation 长度上限须对齐列长](validation-length-caps-191.md) — Prisma MySQL 裸 String 默认 VARCHAR(191) 非 255;validateXxxData 上限超列长会「过校验→写库溢出→500」,改校验器先查 schema 列长(2026-08-25 批3 已全降 191)
- [COS 签名三坑](cos-signature-gotchas.md) — ①Node fetch 禁设 Date/Host,别纳入签名 headerList(否则 COS 403),只签 content-md5/content-type;②SignKey/q-signature/Authorization 别 console.log(600s 窗口可伪造 PUT/DELETE);③一次请求只读一次 config/domain
- [session 文件存储路径穿越+cleanup 死代码](session-file-store-path-traversal.md) — FileSessionStore sessionId 来自 cookie 未净化,`session=../../package` 可读/删任意 .json;三处 cleanup() 死代码致过期会话泄漏,已在 getSessionStore 内 TTL 节流触发
- [urlGuard IPv6 SSRF 判定](ssrf-ipv6-urlguard.md) — Node URL 把 IPv4 映射规范化成 hex `::ffff:a9fe:a9fe`;IPv6 按 CIDR(fe80::/10 全段/fc00::/7)而非字符串前缀;DNS rebinding 仍是开放局限(需 undici dispatcher)
- [markdown music matchArray 索引](markdown-music-matcharray-index.md) — transformMusicLinks 的 replace 回调 args 末 2 个是 offset/string,须 `[match, ...args.slice(0,-2)]` 恢复索引,否则四平台播放器转换整体失效
- [noUncheckedIndexedAccess 下标坑](array-index-trim-noUncheckedIndexedAccess.md) — vue-tsc 开 noUncheckedIndexedAccess,`arr[i].trim()` 报 TS2532;eslint flat config 不查类型,`bunx nuxi typecheck` 才暴露(exit 码经 `| tail` 失真须 grep "error TS");循环下标统一 `(x[i] ?? "").trim()`
- [站点单人运营不变式](single-user-site-invariant.md) — 单人博客,db:init 只种 admin/123456,users 无 role 列;「任意已登录用户」=站主本人,勿把无 role/归属校验当多用户漏洞;mini/links.get 公开 owner 订阅列表是用户确认的有意设计,mini/ 是空 git 子模块
- [Markdown编辑器保存成功但界面回退](markdown-editor-save-revert-writeback.md) — MD 输入→保存→绿色成功但编辑器回编辑前内容;根因是过期 tiptap doc 经 emitMarkdown/onBeforeUnmount 的 writeMarkdownOut 回写覆盖 model;修复=editor→model 回写仅在富文本模式生效 + flush 包 try/catch;复现须全新加载页首次保存(时灵时不灵);DB MySQL LENGTH() 字节 vs JS .length 字符,别误判内容被压缩
- [build-hash.json 与 nitro 路径脆弱性](build-hash-nitro-path.md) — 写 build-hash.json 的只有 postbuild `update-sw-cdn.mjs`(nuxt.config 注释的 build:done hook 是空话,不存在);它从 nitro 产物正则抓 `"buildHash"`,但产物路径随版本变(4.4.8/Nitro2.13 为 chunks/nitro/nitro.mjs,早前 chunks/_/)→硬编码即 ENOENT 被吞→文件静默缺失+sw.js 双斜杠;已改动态定位;核对 hash 用 node 别用 grep(二进制文件)
- [文章详情页 hydration mismatch 首帧 comment↔div](hydration-mismatch-pending-immediate-watch.md) — `[slug]` related-contents 的 `watch(...,{immediate:true})` 在客户端 setup 同步把 relatedContentsPending 置 true(渲染「加载相关文章」div),SSR 被 2026-09-02 cdb045b8 加的 `import.meta.client` 守卫挡回(保持 false→#comment)→首帧 comment↔div node mismatch;prod 只报一句 `Hydration completed but contains mismatches`、dev 才报每个节点+组件栈;修复=watch 去 immediate+onMounted 触发 loadRelatedContents;二维码/小程序码区并非根因;排查要 dev 模式+CDP 抓 console,回归用 git show 看近期是否加 import.meta.client 守卫
