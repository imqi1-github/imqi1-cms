# 旧主题数据迁移指南

本文档说明如何将旧主题的数据库迁移到新系统。

## 迁移前准备

1. **确保 old_theme.sql 文件在项目根目录**
   - 文件路径：`A:\nodejs-imqi1\old_theme.sql`

2. **备份数据库**
   ```bash
   # 推荐在迁移前备份当前数据库
   ```

3. **确认环境变量配置**
   - 确保 `.env` 文件中的数据库配置正确
   - 确保数据库用户有足够的权限

## 迁移内容

迁移脚本会自动迁移以下数据：

- ✅ **用户数据** - 保留用户名、昵称、邮箱、密码等
- ✅ **分类和标签** - 保留所有分类和标签及其关系
- ✅ **文章内容** - 保留所有文章及其元数据
- ✅ **文章关系** - 文章与分类/标签的关联关系
- ✅ **评论数据** - 保留所有评论及其层级关系
- ✅ **友情链接** - 保留所有友情链接
- ✅ **站点配置** - 迁移部分站点配置

## 执行迁移

### 方法一：使用 npm 命令（推荐）

```bash
bun run db:migrate
```

### 方法二：直接使用 tsx

```bash
npx tsx prisma/seed/migrate.ts
```

## 迁移过程

迁移过程中会显示详细的进度信息：

```
🔄 开始迁移旧数据库...
📥 导入旧数据库...
   ✅ 旧数据库导入完成
👤 迁移用户...
   ✅ 迁移了 X 个用户
📁 迁移分类和标签...
   ✅ 迁移了 X 个分类/标签
📝 迁移文章...
   ✅ 迁移了 X 篇文章
🔗 迁移文章关系...
   ✅ 迁移了 X 条关系
💬 迁移评论...
   ✅ 迁移了 X 条评论
🔗 迁移友情链接...
   ✅ 迁移了 X 个友情链接
⚙️  迁移站点配置...
   ✅ 迁移了 X 条配置

✨ 数据迁移完成！

📊 迁移统计：
   - 用户: X
   - 分类/标签: X
   - 文章: X
   - 文章关系: X
   - 评论: X
   - 友情链接: X
   - 配置: X
```

## 字段映射说明

### 文章表 (contents -> post)

| 旧字段 | 新字段 | 转换说明 |
|--------|--------|----------|
| cid | cid | 直接映射 |
| title | title | 直接映射 |
| slug | slug | 直接映射 |
| created (int) | create_time (datetime) | Unix时间戳转Date |
| modified (int) | update_time (datetime) | Unix时间戳转Date |
| text | content | 直接映射 |
| authorId | uid | 直接映射 |
| status | status | 'publish'->1, 其他->0 |
| commentsNum | comment_num | 直接映射 |
| allowComment | allowComment | '1'->true, '0'->false |

### 评论表 (comments -> comment)

| 旧字段 | 新字段 | 转换说明 |
|--------|--------|----------|
| coid | coid | 直接映射 |
| cid | cid | 直接映射 |
| created (int) | created (datetime) | Unix时间戳转Date |
| author | name | 直接映射 |
| authorId | uid | 直接映射 |
| mail | mail | 直接映射 |
| url | link | 字段名变更 |
| text | content | 直接映射 |
| status | status | 'approved'->1, 其他->0 |
| parent | parent_id | 字段名变更 |

### 分类/标签表 (meta -> category)

| 旧字段 | 新字段 | 转换说明 |
|--------|--------|----------|
| mid | mid | 直接映射 |
| name | name | 直接映射 |
| slug | slug | 直接映射 |
| type | type | 'category'->'category', 其他->'tag' |
| description | desc | 字段名变更 |

## 注意事项

1. **密码处理**
   - 旧系统的密码会被重新加密（bcrypt）
   - 如果无法解密，将使用默认密码 `123456`

2. **文章描述**
   - 旧系统没有单独的描述字段
   - 新系统的 `desc` 字段会留空
   - 可以在后台手动补充

3. **浏览量**
   - 旧系统没有浏览量统计
   - 新系统的 `views` 字段默认为 0

4. **图片处理**
   - 文章中的图片路径需要手动检查和更新
   - 封面图片需要重新设置

5. **临时表清理**
   - 迁移过程中会在数据库中创建临时表（imqi1_*）
   - 迁移完成后这些表会保留，可以手动删除

## 迁移后检查

迁移完成后，建议检查以下内容：

1. ✅ 用户登录是否正常
2. ✅ 文章列表是否完整
3. ✅ 分类和标签是否正确关联
4. ✅ 评论是否正常显示
5. ✅ 友情链接是否正确
6. ✅ 站点配置是否需要调整

## 回滚方案

如果迁移出现问题，可以：

1. 删除迁移的数据
2. 恢复之前的数据库备份
3. 重新执行迁移

## 常见问题

### Q: 迁移时报错 "Access denied"
A: 检查数据库用户权限，确保有 CREATE、DROP、INSERT、UPDATE 权限

### Q: 迁移后文章显示异常
A: 检查 Markdown 格式是否兼容，必要时手动调整

### Q: 评论层级关系混乱
A: 检查 `parent_id` 字段是否正确映射

### Q: 密码无法登录
A: 使用默认密码 `123456` 登录后在后台修改

## 技术支持

如遇到问题，请检查：
1. `.env` 文件配置是否正确
2. 数据库服务是否正常运行
3. old_theme.sql 文件是否完整
4. Node.js 版本是否符合要求（推荐 v18+）
