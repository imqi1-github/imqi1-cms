# imqi1-nodejs 项目记忆文件

> 此文件用于记录项目结构和关键信息，方便接手项目时快速了解

## 项目概述

**imqi1-nodejs** 是一个基于 Nuxt 4 构建的博客管理系统后台，采用全栈 TypeScript 开发。

- **开发端口**: 4000
- **开发阶段**: 功能开发中
- **当前状态**: 登录功能已完成，后台 UI 基本完成，核心 API 已实现

---

## 技术栈

### 核心框架
- **Nuxt 4** - Vue 3 全栈框架
- **Vue 3** - Composition API + `<script setup>`
- **TypeScript** - 类型安全
- **PostgreSQL** - 主数据库
- **Prisma** - ORM

### UI 组件
- **shadcn-nuxt** - UI 组件库（风格: new-york）
- **Tailwind CSS v4** - 原子化 CSS
- **Radix Vue** - 无头 UI 原语
- **Lucide Vue** - 图标库
- **@tanstack/vue-table** - 表格组件

### 后端/工具
- **bcrypt** - 密码加密
- **h3** - Nuxt 服务端框架（内置）
- **VueUse** - Vue 组合式工具
- **clsx** + **tailwind-merge** - className 合并
- **class-variance-authority** - 样式变体

---

## 项目结构

```
A:\nodejs-imqi1\
├── app/
│   ├── app.vue                      # 根组件
│   ├── components/                   # 组件目录
│   │   ├── AdminLayout.vue          # 管理后台布局
│   │   └── ui/                      # shadcn UI 组件
│   ├── pages/                       # 页面目录（文件路由）
│   │   ├── login/                   # 登录页
│   │   └── admin/                   # 后台管理页
│   │       ├── index.vue            # 仪表盘首页
│   │       ├── posts/               # 文章管理
│   │       ├── comments/            # 评论管理
│   │       ├── categories/          # 分类管理
│   │       ├── users/               # 用户管理
│   │       ├── links/               # 友链管理
│   │       └── settings/            # 系统设置
│   ├── plugins/                     # Nuxt 插件
│   │   └── auth.client.ts           # 认证客户端插件
│   ├── middleware/                  # 路由中间件
│   │   └── auth.global.ts           # 全局认证中间件
│   ├── types/                       # TypeScript 类型
│   │   └── global.d.ts              # 全局类型定义
│   ├── assets/css/main.css          # 主样式文件
│   └── lib/utils.ts                 # cn() 工具函数
├── server/                          # 服务端 API
│   ├── api/                         # API 路由
│   │   ├── auth/                    # 认证相关
│   │   │   ├── login.post.ts        # 登录接口
│   │   │   └── logout.post.ts       # 登出接口
│   │   └── admin/                   # 管理后台 API
│   │       ├── stats.get.ts         # 统计数据
│   │       ├── posts/               # 文章 CRUD
│   │       ├── comments/            # 评论 CRUD
│   │       ├── categories/          # 分类 CRUD
│   │       ├── users/               # 用户 CRUD
│   │       ├── links/               # 友链 CRUD
│   │       ├── subscribes/          # 订阅管理
│   │       └── settings/            # 系统设置
│   ├── lib/
│   │   └── auth.ts                  # 认证逻辑库
│   └── utils/
│       └── prisma.ts                # Prisma 客户端
├── prisma/
│   ├── schema.prisma                # 数据库模型定义
│   ├── config.ts                    # Prisma 配置
│   └── seed.ts                      # 种子数据
├── components.json                  # shadcn 配置
├── nuxt.config.ts                   # Nuxt 配置
└── package.json
```

---

## 数据库模型 (Prisma Schema)

| 模型 | 表名 | 关键字段 | 说明 |
|------|------|----------|------|
| `User` | users | id, name, mail, password, role | 用户表 |
| `Post` | contents | cid, title, content, status | 文章表 |
| `Comment` | comments | coid, cid, content, status | 评论表 |
| `Category` | metas | mid, name, class | 分类/话题 |
| `PostRelation` | relationships | cid, mid | 文章-分类关联 |
| `Link` | links | id, name, link, enabled | 友情链接 |
| `Subscribe` | subscribes | id, url, name | RSS 订阅 |
| `Meta` | options | id, key, value | 系统设置 |
| `Changelog` | logs | id, class, desc | 更新日志 |

**关系**:
- Post ↔ Category: 通过 PostRelation 多对多
- Post → Comment: 一对多
- Category.class: 'category' | 'topic'

**默认管理员账号**: admin / 123456

---

## API 路由结构

| 路径 | 方法 | 功能 |
|------|------|------|
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/logout` | POST | 用户登出 |
| `/api/admin/stats` | GET | 仪表盘统计数据 |
| `/api/admin/posts` | GET/POST | 文章列表/创建 |
| `/api/admin/posts/[id]` | GET/PUT/DELETE | 文章详情/更新/删除 |
| `/api/admin/comments` | GET | 评论列表 |
| `/api/admin/comments/[id]` | DELETE | 删除评论 |
| `/api/admin/categories` | GET/POST | 分类列表/创建 |
| `/api/admin/categories/[id]` | PUT/DELETE | 更新/删除分类 |
| `/api/admin/users` | GET | 用户列表 |
| `/api/admin/users/[id]` | PUT/DELETE | 更新/删除用户 |
| `/api/admin/links` | GET/POST | 友链列表/创建 |
| `/api/admin/links/[id]` | PUT/DELETE | 更新/删除友链 |
| `/api/admin/subscribes` | GET/POST | 订阅列表/创建 |
| `/api/admin/settings` | GET/PUT | 系统设置 |

---

## 认证机制

- **会话管理**: 基于 Cookie，使用 `session` 库
- **密码加密**: bcrypt 加密存储
- **路由保护**: `app/middleware/auth.global.ts` 全局中间件
- **客户端状态**: `app/plugins/auth.client.ts` 提供响应式认证状态

---

## 开发命令

```bash
# 安装依赖
yarn install

# 启动开发服务器（端口 4000）
yarn dev

# 构建生产版本
yarn build

# 预览生产版本
yarn preview

# 数据库相关
yarn prisma:generate    # 生成 Prisma Client
yarn prisma:migrate     # 运行迁移
yarn prisma:studio      # 打开 Prisma Studio
yarn prisma:seed        # 填充种子数据
yarn db:reset           # 重置数据库（migrate reset + seed）
```

---

## 配置说明

### nuxt.config.ts 关键配置
- devServer.port: 4000
- shadcn-nuxt 模块已集成
- PostCSS + Tailwind + Autoprefixer
- runtimeConfig: 环境变量配置

### components.json 关键配置
- style: "new-york"
- typescript: true
- icon: lucide-vue-next
- 别名: `@` 指向项目根目录

### 环境变量 (.env)
```
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
```

---

## 开发注意事项

### 代码风格
- 使用 `<script setup>` 语法
- 优先使用 Composition API
- 统一使用 `cn()` 函数合并 Tailwind 类名

### 样式系统
- 支持暗色模式（通过 CSS 变量）
- 主题色: zinc 色系
- 响应式设计，移动端优先

### 已实现功能
- ✅ 登录/登出功能
- ✅ 认证中间件和会话管理
- ✅ 仪表盘统计数据
- ✅ 文章 CRUD API
- ✅ 评论管理 API
- ✅ 分类管理 API
- ✅ 用户管理 API
- ✅ 友链管理 API
- ✅ 种子数据脚本

### 待完善功能
- ⏳ 前端页面与 API 的数据对接
- ⏳ 表单验证
- ⏳ 文章富文本编辑器
- ⏳ 评论审核功能
- ⏳ 文件上传功能

---

## Git 状态参考

```
当前分支: master
最近提交:
  - 登录功能
  - 登录功能30%
  - 初始化示例数据
  - 后台初具雏形
  - 组件
```

---

## 快速上手步骤

1. 配置 `.env` 文件中的 `DATABASE_URL`
2. 运行 `yarn prisma:migrate` 创建数据库表
3. 运行 `yarn prisma:seed` 填充示例数据
4. 运行 `yarn dev` 启动开发服务器
5. 访问 `http://localhost:4000/login`，使用 admin/123456 登录
