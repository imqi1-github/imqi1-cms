# imqi1-nodejs 项目记忆文件

> 此文件用于记录项目结构和关键信息，方便接手项目时快速了解

## 项目概述

**imqi1-nodejs** 是一个基于 Nuxt 4 构建的现代化后台管理系统（CMS），主要用于博客/文章管理。

- **开发端口**: 4000
- **开发阶段**: 早期开发阶段
- **当前状态**: UI 基本完成，逻辑待实现

---

## 技术栈

### 核心框架
- **Nuxt 4** - Vue 3 全栈框架
- **Vue 3** - Composition API + `<script setup>`
- **TypeScript** - 类型安全
- **PostgreSQL** - 主数据库
- **Prisma** - ORM

### UI 组件
- **shadcn-nuxt** - UI 组件库（风格: new-york）
- **Tailwind CSS v4** - 原子化 CSS
- **Radix Vue** - 无头 UI 原语
- **Lucide Vue Next** - 图标库

### 工具库
- **VueUse** - Vue 组合式工具
- **clsx** + **tailwind-merge** - className 合并
- **class-variance-authority** - 样式变体

---

## 项目结构

```
A:\imqi1-nodejs\
├── app/
│   ├── app.vue                    # 根组件
│   ├── pages/                     # 页面目录（文件路由）
│   │   ├── login/index.vue        # 登录页
│   │   └── admin/                 # 后台管理页
│   │       ├── index.vue          # 仪表盘首页
│   │       ├── posts/             # 文章管理
│   │       ├── comments/          # 评论管理
│   │       ├── categories/        # 分类管理
│   │       ├── users/             # 用户管理
│   │       ├── links/             # 友链管理
│   │       └── settings/          # 系统设置
│   ├── assets/css/main.css        # 主样式文件
│   ├── lib/utils.ts               # cn() 工具函数
│   └── server/utils/prisma.ts     # Prisma 客户端
├── prisma/schema.prisma           # 数据库模型定义
├── components.json                # shadcn 配置
├── nuxt.config.ts                 # Nuxt 配置
└── package.json
```

---

## 数据库模型 (Prisma Schema)

| 模型 | 说明 | 关键字段 |
|------|------|----------|
| `Post` | 文章 | title, content, status, publishedAt |
| `Comment` | 评论 | content, postId, authorId |
| `Category` | 分类 | name, slug |
| `User` | 用户 | username, email, role |
| `Link` | 友链 | name, url, status |
| `Changelog` | 更新日志 | version, content |
| `Meta` | 元数据键值对 | key, value |
| `Subscription` | 订阅 | email, verified |

**关系**:
- Post ↔ Category: 多对多
- Post → Comment: 一对多
- User → Post/Comment: 一对多

---

## 路由结构

| 路径 | 页面 | 状态 |
|------|------|------|
| `/login` | 登录页 | UI 完成，无逻辑 |
| `/admin` | 仪表盘 | 带模拟数据 |
| `/admin/posts` | 文章管理 | 基础结构 |
| `/admin/comments` | 评论管理 | 基础结构 |
| `/admin/categories` | 分类管理 | 基础结构 |
| `/admin/users` | 用户管理 | 基础结构 |
| `/admin/links` | 友链管理 | 基础结构 |
| `/admin/settings` | 系统设置 | 基础结构 |

---

## 开发命令

```bash
# 安装依赖
bun install  # 或 yarn install

# 启动开发服务器（端口 4000）
bun run dev

# 数据库相关
bun run prisma:generate    # 生成 Prisma Client
bun run prisma:migrate     # 运行迁移
bun run prisma:studio      # 打开 Prisma Studio
bun run prisma:seed        # 填充数据
```

---

## 配置说明

### nuxt.config.ts 关键配置
- devServer.port: 4000
- shadcn-nuxt 模块已集成
- PostCSS + Tailwind + Autoprefixer

### components.json 关键配置
- style: "new-york"
- typescript: true
- icon: lucide
- 别名: `@` 指向项目根目录

---

## 开发注意事项

### 代码风格
- 使用 `<script setup>` 语法
- 优先使用 Composition API
- 统一使用 `cn()` 函数合并 Tailwind 类名

### 待实现功能
1. 登录逻辑
2. API 路由（/server/api）
3. 数据库迁移
4. 状态管理（认证、用户信息）
5. 表单验证

### 样式系统
- 支持暗色模式（通过 CSS 变量）
- 主题色: slate 色系
- 响应式设计，移动端优先

---

## Git 状态参考

```
当前分支: master
已修改:
  - app/assets/css/main.css
  - app/pages/login/index.vue
  - components.json
  - nuxt.config.ts

最近提交:
  - 登录页面样式（还没有逻辑）
  - 添加 shadcn
  - 后台雏形
```

---

## 快速上手步骤

1. 配置 `.env` 文件中的 `DATABASE_URL`
2. 运行 `bun run prisma:migrate` 创建数据库表
3. 运行 `bun run dev` 启动开发服务器
4. 访问 `http://localhost:4000/login`
